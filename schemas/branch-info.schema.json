{
    "description": "This is the file which is used to provide branch metadata",
    "title": "branch-info.json",
    "type": "object",

    "properties": {
        "topic": {
            "type": "string",
            "minLength": 5,
            "description": "The topic of the lesson",
            "examples": ["Intro to bash"]
        },

        "description": {
            "type": "string",
            "minLength": 5,
            "description": "The optional detailed explanation of the lesson, should be of reasonable length",
            "examples": ["The lesson teaches the fundamentals of bash 😂"]
        },

        "lastUpdated": {
            "type": "string",
            "format": "YYYY-MM-DD",
            "description": "The optional date at which the branch was last edited. By default, the period when the changes in the `branch-info.txt` file were pushed is used",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "examples": ["YYYY-MM-DD"]
        },

        "author": {
            "type": "string",
            "description": "The optional username of the person who created the lesson. By default, its the person who created the branch",
            "minLength": 1,
            "examples": ["itsbluejelly"]
        }
    },

    "required": ["topic"],
    "additionalProperties": false
}